<%- include('../layouts/header') %>
<!-- FORMULÁRIO CADASTRO/EDIÇÃO -->
           
	<div class="main">
		<header>
			<div>
				<h1>Gerenciar Computadores</h1>
				<p>Cadastro e edição de ativos</p>
			</div>
		</header>


            <div class="card card-form">
                <div class="card-header">
                    <h5 class="card-title">Editar Ativos</h5>
                </div>
                <div class="card-body">
                    <form id="formAativo" class="row g-2">
                        <div class="col-md-4">
                            <label for="ip" class="form-label">IP</label>
                            <input type="text" class="form-control" id="ip" name="ip" placeholder="192.168.1.100">
                        </div>

                        <div class="col-md-4">
                            <label for="idComputador" class="form-label">ID do Computador</label>
                            <input type="text" class="form-control" id="idComputador" name="idComputador"
                                placeholder="ID único">
                        </div>

                        <div class="col-md-4">
                            <label for="nomeMaquina" class="form-label">Nome da Máquina</label>
                            <input type="text" class="form-control" id="nomeMaquina" name="nomeMaquina"
                                placeholder="Ex: PC-ADM-001">
                        </div>

                        <div class="col-md-4">
                            <label for="macAddress" class="form-label">MAC Address</label>
                            <input type="text" class="form-control" id="macAddress" name="macAddress"
                                placeholder="00:1A:2B:3C:4D:5E">
                        </div>

                        <div class="col-md-4">
                            <label for="patrimonio" class="form-label">Patrimônio</label>
                            <input type="text" class="form-control" id="patrimonio" name="patrimonio"
                                placeholder="Número do patrimônio">
                        </div>

                        <div class="col-md-4">
                            <label for="numeroSerie" class="form-label">Número de Série</label>
                            <input type="text" class="form-control" id="numeroSerie" name="numeroSerie"
                                placeholder="Número de série">
                        </div>

                        <div class="col-md-4">
                            <label for="setor" class="form-label">Setor</label>
                            <input type="text" class="form-control" id="setor" name="setor"
                                placeholder="Setor/Departamento">
                        </div>

                        <div class="col-md-4">
                            <label for="laboratorio" class="form-label">Laboratório</label>
                            <input type="text" class="form-control" id="laboratorio" name="laboratorio"
                                placeholder="Lab/Sala">
                        </div>

                        <div class="col-md-4">
                            <label for="so" class="form-label">SO</label>
                            <select class="form-control" id="so" name="so">
                                <option value="">Selecione o SO</option>
                                <option value="Windows 11">Windows 11</option>
                                <option value="Windows 10">Windows 10</option>
                                <option value="Kali Linux 10.05">Kali Linux 10.05</option>
                                <option value="Ubuntu">Ubuntu</option>
                                <option value="macOS">macOS</option>
                                <option value="Outro">Outro</option>
                            </select>
                        </div>

                        <!-- Observações removido conforme solicitado -->

                        <div class="col-12">
                            <div class="button-group">
                                <button type="submit" class="btn btn-primary btn-save">
                                    Salvar Ativo
                                </button>
                                <button type="reset" class="btn btn-secondary btn-clear">
                                    Limpar
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>

            <!-- SEÇÃO DE FILTROS E LISTA -->
            <div class="card card-list mt-4">
                <div class="card-header">
                    <h5 class="card-title">Lista de ativos</h5>
                </div>
                <div class="card-body">
                    <!-- FILTROS -->
                    <div class="filters-section mb-4">
                        <div class="row g-2">
                            <div class="col-md-6">
                                <label for="filterStatus" class="form-label">Filtro de status</label>
                                <select class="form-control" id="filterStatus" name="filterStatus">
                                    <option value="">Todos os status</option>
                                    <option value="ativo">Ativo</option>
                                    <option value="inativo">Inativo</option>
                                    <option value="manutencao">Manutenção</option>
                                </select>
                            </div>

                            <div class="col-md-6">
                                <label for="filterSetor" class="form-label">Filtro de setor</label>
                                <select class="form-control" id="filterSetor" name="filterSetor">
                                    <option value="">Todos os setores</option>
                                    <% if (setores && setores.length > 0) { %>
                                        <% setores.forEach(s => { %>
                                            <option value="<%= s.nome %>"><%= s.nome %></option>
                                        <% }); %>
                                    <% } %>
                                </select>
                            </div>
                        </div>
                    </div>

                    <!-- TABELA -->
                    <div class="table-responsive">
                        <table class="table table-hover table-striped">
                            <thead class="table-header">
                                <tr>
                                    <th>ID</th>
                                    <th>Nome/IP</th>
                                    <th>MAC</th>
                                    <th>Setor</th>
                                    <th>SO</th>
                                    <th>Ações</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (ativos && ativos.length > 0) { %>
                                    <% ativos.forEach(ativo => { %>
                                        <tr>
                                            <td><%= ativo.id_ativo %></td>
                                            <td>
                                                <strong><%= ativo.nome_maquina || '-' %></strong><br>
                                                <small><%= ativo.ip || '-' %></small>
                                            </td>
                                            <td><%= ativo.mac_address || '-' %></td>
                                            <td><%= ativo.setor || '-' %></td>
                                            <td><%= ativo.sistema_operacional || '-' %></td>
                                            <td>
                                                <button class="btn btn-sm btn-danger btn-action" title="Editar">
                                                    <i class="fa-solid fa-pen"></i>
                                                </button>
                                                <button class="btn btn-sm btn-danger btn-action" title="Deletar">
                                                    <i class="fa-solid fa-trash"></i>
                                                </button>
                                            </td>
                                        </tr>
                                    <% }); %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="6" class="text-center">Nenhum ativo encontrado</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="/js/script.js" defer></script>

<!-- SCRIPT PARA MARCAR LINK ATIVO NO MENU -->
<script>
    document.addEventListener('DOMContentLoaded', () => {
        const navLinks = document.querySelectorAll('.sidebar nav a.nav-link');
        const currentPath = window.location.pathname;

        navLinks.forEach(link => {
            const href = link.getAttribute('href');
            // Remove classe active de todos
            link.classList.remove('active');
            // Verifica se a URL atual corresponde ao link
            if (currentPath === href || currentPath.startsWith(href + '/')) {
                link.classList.add('active');
            }
        });
    });
</script>

</body>
</html>